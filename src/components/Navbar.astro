---
interface Props {
    brandName?: string;
    phone?: string;
}

const {brandName = "CompanyName", phone = "+91 98765 43210"} = Astro.props;

const formattedPhone = phone.replace(/[0-9+]/g, '');
---

<header class="sticky top-0 z-50 w-full bg-ktx-mantle backdrop-blur-md border-b border-ktx-crust">
    <div class="max-w-5xl mx-auto px-4 h-16 flex items-center justify-between">
        <a href="#" class="flex items-center gap-2 group">
            <div class="w-9 h-9 bg-ktx-blue rounded-lg flex items-center justify-center text-white font-bold text-xl shadow-md shadow-blue-600/30 group-hover:bg-blue-500 transition">
                ⚡            
            </div>
            <span class="text-lg font-bold text-ktx-mauve tracking-tight group-hover:text-blue-400 transition">
                {brandName}
            </span>
        </a>

        <!-- Navigation Links (Hidden on small mobile screens) -->
        <nav class="hidden md:flex items-center gap-6 text-sm font-medium">
            <a href="#services" class="hover:text-ktx-overlay transition">Services</a>
            <a href="#about" class="hover:text-ktx-overlay transition">About Us</a>
            <a href="#contact" class="hover:text-ktx-overlay transition">Contact Us</a>
            <a href="/blog/" class="hover:text-ktx-overlay transition">Blogs</a>
        </nav>

        <!-- Phone CTA Button -->
        <a href={`tel:${formattedPhone}`} class="inline-flex items-center gap-2 bg-ktx-blue text-white font-semibold text-sm px-4 py-2 rounded-xl shadow-md shadow-blue-600/20 transition duration-200">
            <!-- Call Icon SVG -->
            <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" class="size-6">
                <path fill-rule="evenodd" d="M1.5 4.5a3 3 0 0 1 3-3h1.372c.86 0 1.61.586 1.819 1.42l1.105 4.423a1.875 1.875 0 0 1-.694 1.955l-1.293.97c-.135.101-.164.249-.126.352a11.285 11.285 0 0 0 6.697 6.697c.103.038.25.009.352-.126l.97-1.293a1.875 1.875 0 0 1 1.955-.694l4.423 1.105c.834.209 1.42.959 1.42 1.82V19.5a3 3 0 0 1-3 3h-2.25C8.552 22.5 1.5 15.448 1.5 6.75V4.5Z" clip-rule="evenodd" />
            </svg>
            <span>Call now</span>
        </a>
    </div>
</header>


